import 'package:flutter/material.dart';

class TicketInfoPage extends StatelessWidget {
  static const routeName = '/ticket-info';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
